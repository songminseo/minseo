#include <stdio.h>

int main(void)
{
	int x;
	int y;
	int sum;

	printf("사각형 넓이 구하기\n");
	printf("너비 입력 :");
	scanf("%d", &x);
	printf("높이 입력:");
	scanf("%d", &height);
	printf("넓이 : %d\n", weight * height);

	return 0;
}